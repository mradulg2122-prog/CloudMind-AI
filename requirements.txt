# CloudMind AI — Python Dependencies (v5 — Production Grade)
# Install with: pip install -r requirements.txt
# Pin all versions for reproducible builds

# ── Core Backend ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
aiofiles==23.2.1               # Async file I/O for background tasks
python-dotenv==1.0.1           # Load .env files for local development

# ── ML & Data ─────────────────────────────────────────────────────────────────
scikit-learn==1.4.2
joblib==1.4.2
numpy==1.26.4
pandas==2.2.2

# ── Authentication & Security ─────────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT token creation & verification
passlib[bcrypt]==1.7.4             # Password hashing with bcrypt
python-multipart==0.0.9            # Required for OAuth2 form data

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30                 # ORM for SQLite / PostgreSQL
alembic==1.13.1                    # Database migration tool
# psycopg2-binary==2.9.9           # Uncomment for PostgreSQL support

# ── Rate Limiting ─────────────────────────────────────────────────────────────
slowapi==0.1.9                     # Rate limiting middleware for FastAPI

# ── PDF Export (optional) ─────────────────────────────────────────────────────
# reportlab==4.2.0                 # Uncomment for rich PDF export support

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
httpx==0.27.0                      # Required by FastAPI TestClient
pytest-cov==5.0.0                  # Coverage reporting
pytest-asyncio==0.23.6             # Async test support
anyio==4.3.0                       # Async compatibility layer